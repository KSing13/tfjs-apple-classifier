# Supervised Learning: Apple-MNIST CNN Classifier

A basic Convolutional Neural Network (CNN) for classifying images of apples (e.g., Green Apple vs. Red Apple) using TensorFlow.js for Node.js.

## Installation

```
npm install @tensorflow/tfjs-node
```
